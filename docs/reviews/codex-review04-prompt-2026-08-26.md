# Codex レビュー依頼 R4 — AKB48 Mail レスキューツール(2026-08-26)

## 依頼

添付の 3 パケット(同じフォルダの `akbmail-review-r4-CODE-20260826.md` / `-TESTS-` / `-DOCS-`)を読み、**敵対的な立場で**問題点を挙げてください。
形式は前回と同じ「重大度 / 該当箇所 / 何が起きるか / 直し方」。称賛は不要です。

**お願い**: レビュー結果の全文を、このリポジトリの `docs/reviews/codex-review04-result-2026-08-26.md` として保存してください(依頼側の環境から Windows のユーザーフォルダは参照できないため)。

## 前回(R3)からの変更

R3 の 5 件を是正しました(DOCS パケットの `docs/reviews/codex-review03-triage-2026-08-26.md`)。

- `src/core/ocrQueue.ts`: 候補をグレー 8bit 配列(≈0.5MB)にして全体上限 64。待ち中の候補は区間ごと 3 まで。失敗後は `minSpacing`(3 フレーム)以上離れた候補のみ受け付け。連続 `maxConsecutiveErrors`(3)回までのエラーは候補失敗として続行。長尺(40 区間)公平性・エラー継続・間隔・待ち上限のテストを追加
- `src/App.tsx`: 致命エラー時に Worker を作り直す(`workerGen`)
- `src/core/store.ts`: `HEARTBEAT_STALE_MS` を 5 分に(バックグラウンドタブのタイマー抑制対策)
- `src/core/frameGray.ts`: `cropGrayUpscaledArray`(グレー配列の切り出し)

## 特に見てほしい点

1. **R3 是正の妥当性**: 待ち 3 / 全体 64 / 間隔 3 の組み合わせで、失敗区間の 8 回試行が「実行中に補充される」前提が成り立っているか(区間が閉じた後に detail フレームが来ないと補充されない点の影響)。`minSpacing` が最初の候補(区間先頭)を弾く経路が無いか
2. **Worker 再生成**: `workerGen` 変更で旧 Worker を terminate した後、旧 Worker からの遅延メッセージ(`done`/`output`)が新しいジョブに混入しないか。キュー(`queue`)の次の動画が古い状態で開始されないか
3. **INV-1/2/3** の再確認(変更箇所に限らず)
4. **UI の状態遷移**: 中止・自動中止・エラー・完了・復元の各経路で `busyRef` / `status` / IndexedDB の status が一貫するか
5. **テストの網羅**: 今回追加分が仕様を固定しているか。未網羅で最も危険な箇所は何か

## 制約

- レビュー結果は内部利用のみ。パケットの内容を外部に転載しない。
- 修正コードの提案は差分または要点で十分。実装は依頼側で行う。
