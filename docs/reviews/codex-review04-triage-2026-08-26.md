# Codex レビュー R4 — 精査と是正記録(2026-08-26、内部用)

対象: R4(結果全文 `codex-review04-result-2026-08-26.md`)。High 2・Medium 3、すべて妥当と判断し是正。

| # | 指摘 | 是正 |
|---|---|---|
| 1 High | 失敗区間は区間閉鎖後に補充されず最大 4 回で終わる | 区間ごとの待ち上限を 8(= 試行上限)に。候補はグレー配列(≈0.5MB)なので 8 件でも 4MB。全体上限 64 到達時は「待ちが最も多い**他**区間の末尾」を退避(自区間の候補を退避してまでは入れない)。「全候補を同期投入して区間を閉じてから OCR を解放 → 8 回試行」のテストを追加 |
| 2 High | Worker 応答にジョブ世代が無く旧 Worker の遅延イベントが混入 | 依頼に `jobId` を付け、Worker は全応答にエコーバック。App は `acceptsMessage()` で現在ジョブと一致しない応答を副作用の前に捨てる(ログに記録)。単体テスト+「中止 → 直後に次の動画」のブラウザ E2E(`scripts/e2e-cancel.mjs`: 2 本目が完了・出力 10・混入 0・履歴は 1 本目=中断/2 本目=完了) |
| 3 Medium | `messageerror` が致命エラー経路を通らない | `fail()` に統一(ジョブ error、Worker 再生成) |
| 4 Medium | 受け入れ判定前に切り出しコストを払う | `OcrQueue.canAccept(seg, index)` を切り出し前に呼ぶ。切り出しで作る中間 canvas は即 0 サイズ化。実録画 E2E で dropped が 3/1 → 0/0 に |
| 5 Medium | 世代交代・状態遷移のテストが無い | 上記の単体テスト・E2E を追加。React 内部の Worker 注入テストは未実装(残課題: React Testing Library 導入が前提) |

実録画 E2E: 31 秒版 日時 6/7・24 秒版 8/10 を維持。
残課題: 長時間入力での JS heap / canvas backing memory のピーク計測(INV-3 の実証)、React コンポーネントの Worker 注入テスト。
