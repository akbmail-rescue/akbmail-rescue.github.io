# Codex レビュー R6 — 精査と是正記録(2026-08-26、内部用)

対象: R6(結果全文 `codex-review06-result-2026-08-26.md`)。High 1・Medium 3、すべて妥当と判断し是正。収束判定は「未収束」だったため R7 で再確認する。

| # | 指摘 | 是正 |
|---|---|---|
| 1 High | 「飽和時でも各区間最低 4 回」は多数区間で成立しない | 保証を条件付きで正確に定義(要件 F-5): 同時未処理区間 ≤21 → 待ち ≥3(最大 8 回)、≤64 → 待ち ≥1、それ以上 → **新しい区間の最初の候補は必ず受け入れ、最も古い区間の待ちを捨てる**(退避順 (a) 待ち >3 の他区間 → (b) 自区間 <3 なら待ち ≥2 の他区間 → (c) 自区間 0 なら最古の他区間)。21/64/65/100 区間を投入して attempts まで完走させるテストを追加 |
| 2 Medium | 後始末失敗でも cached instance を保持し FS 残骸が蓄積 | 後始末の各操作の失敗を記録し、1 つでも失敗したらログに残して `discardCached()`(処理成功と後始末成功を別管理) |
| 3 Medium | 2 本連続 E2E が旧「完了」で即通過し得る | 各投入で履歴件数の増加と「読み込み中/解析中」への遷移を待ってから終端を待つ。履歴 2 件とも完了・各ジョブ出力 >0 を検証し、失敗時は終了コード 1 |
| 4 Medium | 復元の DB 読み込み失敗後も空の結果を「完了」表示 | 先に `listOutputs` を読み、成功かつ世代一致のときだけ画面を一括更新。失敗時は直前の表示を保ち「復元エラー: <ファイル名>」を明示。復元中は「復元中: <ファイル名>」 |

E2E: HEVC 2 本連続(ffmpeg.wasm)PASS、中止→次動画 PASS、復元 PASS、実録画 31 秒版 6/7・24 秒版 8/10 維持。
残課題(記録済み・未実装): メモリ計測 CI、Fake Worker 注入テスト、Service Worker 事前キャッシュ。
