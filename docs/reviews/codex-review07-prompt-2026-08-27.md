# Codex レビュー依頼 R7 — AKB48 Mail レスキューツール(2026-08-27)

## 依頼

添付の 3 パケット(同じフォルダの `akbmail-review-r7-CODE-20260827.md` / `-TESTS-` / `-DOCS-`)を読み、**敵対的な立場で**問題点を挙げてください。
形式はこれまでと同じ「重大度 / 該当箇所 / 何が起きるか / 直し方」。称賛は不要です。
結果の全文は `docs/reviews/codex-review07-result-2026-08-27.md` として保存してください。

## 前回(R6)からの変更

R6 の 4 件を是正しました(DOCS パケットの `docs/reviews/codex-review06-triage-2026-08-26.md`)。

- `src/core/ocrQueue.ts`: 保証を条件付きに定義(≤21 区間: 待ち ≥3 / ≤64: ≥1 / 超過: 新区間の最初の候補を必ず受け入れ最古区間を捨てる)。`findVictim()` に集約し `canAccept`/`offer` で共有。21/64/65/100 区間の完走テスト
- `src/core/ffmpegExtract.ts`: 後始末の各操作の失敗を記録し、1 つでも失敗すれば cached を破棄(ログ出力)
- `src/App.tsx`: 復元は読み込み成功後に一括更新、失敗時は「復元エラー」表示、復元中は「復元中」表示
- `scripts/e2e-two.mjs`: 遷移(履歴増加+読み込み中/解析中)を待ってから終端を待つ。終了コードで結果を返す
- `docs/requirements.md` F-5 の保証条件を更新

記録済み残課題 3 件(メモリ計測 CI、`addInitScript` の Fake Worker 注入テスト、Service Worker 事前キャッシュ)は未実装のままです。

## 特に見てほしい点

1. R6 是正が閉じているか(特に `findVictim` (c) の「最古区間から捨てる」が、実行中区間や found 済み区間を誤って選ばないか、Map の挿入順が本当に「古い区間」か)
2. 復元の一括更新: `resetView()` を成功後に呼ぶ順序で、`URL.revokeObjectURL` と新しい object URL の生成が競合しないか。復元中に Worker 応答が届く経路(`jobRef` は前ジョブのまま)の副作用
3. **収束判定**: 記録済み残課題 3 件以外に指摘が無ければ、その旨を明記してほしい。残るなら、それが「新規」か「過去の是正の取りこぼし」かを区別してほしい

## 制約

- レビュー結果は内部利用のみ。パケットの内容を外部に転載しない。
- 修正コードの提案は差分または要点で十分。実装は依頼側で行う。
