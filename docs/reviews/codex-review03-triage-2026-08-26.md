# Codex レビュー R3 — 精査と是正記録(2026-08-26、内部用)

対象: R3(R2 是正後)。High 2・Medium 3。R2 の非送信・動的 drain・patchJob・adb 削除前検証は閉じたと判定された。
全文はオーナー環境(Windows)にあり当方から参照不可のため、要約と 4 件のコメント位置に基づいて是正した。

| # | 指摘 | 是正 |
|---|---|---|
| P1 | 長尺で OCR 待ち行列が先行区間で埋まり後続が unknown に偏る | 候補をグレー 8bit 配列(≈0.5MB)にして全体上限 64(≈32MB)。待ち中の候補は区間ごと 3 まで(失敗区間が占有しない)。失敗後は 3 フレーム(0.5 秒)以上離れた候補のみ受け付け無駄打ちを減らす。40 区間・OCR が到着より遅い状況で全区間 ≥1 回試行されるテストを追加 |
| P1 | 単発の Tesseract タイムアウトで残り全区間の OCR が停止 | 連続 3 回までは候補失敗として続行(認識側はワーカー再生成)。テスト追加 |
| P2 | Worker 致命エラー後に同じ Worker を再利用 | `fail()` で Worker を作り直す(`workerGen`) |
| P2 | heartbeat 停滞だけで実行中ジョブを中断扱い | バックグラウンドタブのタイマー抑制(最長 1 分間隔)を考慮し停滞判定を 5 分に |
| P2 | 上記がテスト・CI で未網羅 | ocrQueue のテスト 4 件追加(長尺の公平性・エラー継続・間隔・待ち上限)。Worker 再生成は UI 統合のためテスト無し(残課題) |

実録画 E2E: 31 秒版 日時 6/7、24 秒版 8/10(R2 以前と同じ)、dropped は 3 / 1 に減少。
