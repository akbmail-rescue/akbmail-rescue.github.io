# Codex レビュー R5 — 精査と是正記録(2026-08-26、内部用)

対象: R5(結果全文 `codex-review05-result-2026-08-26.md`)。High 2・Medium 3、すべて妥当と判断し是正。

| # | 指摘 | 是正 |
|---|---|---|
| 1 High | 全体飽和時の末尾退避で連続失敗区間の 8 回試行と最新候補が失われる | 仕様を「各区間最大 8 回、飽和時は他区間の待ちを 3 → 1 の順に削り、新しい区間にも必ず 1 候補」と明記(要件 F-5)。退避は末尾ではなく**中間**(先頭と最新を保持)。自区間が最低保持数(3)以上なら他区間を 3 未満に削らない。OCR 停止のまま 20 区間 / 60 区間 × 8 候補を投入する分布テストを追加(`waitingIndices` で先頭・最新の残存を検証) |
| 2 High | 復元の IndexedDB 読み込み中に次動画を開始でき混入する | `restoring` 状態と復元世代 `restoreGenRef`。復元中は投入・開始・削除を無効化、`await` 後にジョブ ID と世代を再確認し不一致なら破棄。開始・削除・投入は進行中の復元を無効化 |
| 3 Medium | 旧 Worker の DOM `error`/`messageerror` が世代判定外 | 全ハンドラ冒頭で `workerRef.current === w` を確認し、旧インスタンスのイベントを無視。effect の cleanup で `workerRef` を null にしてから terminate |
| 4 Medium | ffmpeg.wasm の固定ディレクトリ・残留ファイルが 2 本目と衝突 | 呼び出しごとに一意な `/input_N` `/out_N`。finally で出力ファイル・ディレクトリ・マウントを除去。異常終了時は cached インスタンスを terminate して破棄。E2E `scripts/e2e-two.mjs`(HEVC 2 本連続、両方 ffmpeg.wasm 経路で完了)を追加 |
| 5 Medium | 「初回表示後はオフラインでも動作」が実装と不一致 | ガイド文言を「必要な部品は最初の解析時に読み込まれ、その後はオフラインでも同じ処理ができる」に、「動画の大きさに上限なし」を「明示的な上限は設けていない」に修正。要件 NF-5 に注記(Service Worker の事前キャッシュは将来対応) |
| 補足 | stitch タイルが 128MB を最大 1 タイル超え得る | 最終タイルを上限までの残り行数で確保 |

提案(メモリ測定案・addInitScript による Fake Worker 注入テスト案)は採用予定として残課題に記録。
実録画 E2E: 31 秒版 日時 6/7・24 秒版 8/10 を維持。中止→次動画、HEVC 2 本連続も PASS。
