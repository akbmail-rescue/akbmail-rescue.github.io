/**
 * 録画手順ガイド(要件 §5)。精度目標(§9)はこの手順に従った「準拠入力」に対して定義される。
 */
export function RecordingGuide() {
  return (
    <div className="guide">
      <h3>おすすめ: 連続スクロール録画(タップ操作なし)</h3>
      <p>
        AKB48 Mail アプリでは、メール本文を末端まで引っ張ると次のメールに切り替わります。これを使うと 1 通あたり約 2.4 秒で録画できます(1,000 通 ≈ 40 分)。
      </p>
      <ol>
        <li>最初のメール(最新または最古)を開いて、画面収録を開始する</li>
        <li>ゆっくりドラッグでスクロールし、メールの末尾で引っ張って次のメールへ進む</li>
        <li>
          <strong>ページが切り替わったら 2 秒静止</strong>する(本文の読み込み待ち。読み込み中の白い画面が「1 通の区切り」の目印になるので、この静止は精度のために必須)
        </li>
        <li>以降くり返し。手が疲れたら録画を止めて分割してかまいません(複数の動画をまとめて投入できます)</li>
      </ol>

      <h3>共通のルール</h3>
      <ul>
        <li>
          長いメールは<strong>ゆっくりスクロール</strong>(フリックせずドラッグ)し、末端で 2 秒静止
        </li>
        <li>画像の全画面表示は開いてもかまいませんが、画像の保存はアプリの↓ボタンをおすすめします</li>
        <li>1 本の動画は 5 分以内を推奨(長い場合は分割して録画)</li>
        <li>画面の明るさ最大、ダークモード OFF、通知バナー OFF、着信は機内モード推奨</li>
      </ul>

      <h3>代わりの方法: 一覧からタップして録画</h3>
      <p>連続スクロールが使いにくい場合は、一覧からメールを開いて 2 秒静止 → 戻る、をくり返す方法でも処理できます(1 通あたり約 4.4 秒)。</p>

      <h3>iPhone で放置録画する(AssistiveTouch)</h3>
      <p>
        「設定 → アクセシビリティ → タッチ → AssistiveTouch → 新規ジェスチャを作成」で「ゆっくり上方向にドラッグ → 2〜3 秒待つ」を 1 動作として登録し、それを連続再生すると、手を離したまま連続スクロール録画ができます。長文メールで 1 回のドラッグで末端に届かなくても、同じドラッグのくり返しで自然に進みます。
      </p>

      <h3>Android の場合</h3>
      <p>
        Android 標準の画面収録で同じ手順です。パソコンと USB でつなげる場合は、録画とスクロールを自動でくり返すスクリプトを用意しています:{' '}
        <a href="https://github.com/akbmail-rescue/akbmail-rescue.github.io/tree/main/tools/android-autoscroll" target="_blank" rel="noreferrer">
          Android 自動スクロール録画の手順
        </a>
      </p>

      <h3>全部は難しいときの優先順</h3>
      <p>スター済み → 記念日・イベント期 → 直近から遡る、の順をおすすめします。</p>

      <h3>対応環境</h3>
      <ul>
        <li>PC / スマートフォンの最新の Chrome / Edge / Safari</li>
        <li>iPhone の画面収録(HEVC)を Windows の Chrome で読み込む場合、パソコンによっては解析に時間がかかります(自動で互換モードに切り替わります)</li>
        <li>動画・画像・文字情報は端末の外に送信されません。初回表示後はオフラインでも動作します</li>
      </ul>
    </div>
  )
}
